# Netlify configuration for All Structure Maintenance

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Function configuration
[functions]
  node_bundler = "esbuild"
